{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core APIs plus the folder picker. Opening folders and the clipboard go through Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
